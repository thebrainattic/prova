on: push
name: Publish Website over FTPS 
jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
      with:
        fetch-depth: 2

    - name: FTP-Deploy-Action
      uses: SamKirkland/FTP-Deploy-Action@3.0.0
      with:
        ftp-server: ftps://ftp.cv19ing20.altervista.org:21/Repository
        ftp-username: cv19ing20
        ftp-password: CuYSm5BHW735
        git-ftp-args: --insecure # if your certificate is setup correctly this can be removed
